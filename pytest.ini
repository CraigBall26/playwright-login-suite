# Registering custom markers for pytest.
# Keeps pytest output clean by avoiding warnings about unknown markers.

[pytest]
markers =
    login: marks login tests
    negative: Negative test scenarios validating error handling.
    environment: Marks tests that simulate environmental conditions (e.g., slow network)
    framework: Framework-level structural tests for locator validation
    positive: marks positive test cases

pythonpath = .

